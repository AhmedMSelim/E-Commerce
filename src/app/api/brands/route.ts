import { NextRequest, NextResponse } from "next/server";

// Route Handler ==> Method & Api % Data
export function GET(req: NextRequest) {
  // Some Logic
  //Write Queries
  return NextResponse.json({});
}
